export class Path {
    idPath: number;
    colorCode: string;

    constructor(
        in_idPath: number,
        in_colorCode: string
    ) {
        this.idPath=in_idPath;
        this.colorCode=in_colorCode;
    }
}